;;; package --- Summary

;;; Commentary:
;;
;; early-init.el --- Emacs configuration file that is loaded before the
;; package system and GUI is initialized
;; --------------------------------------

;;; Code:

(menu-bar-mode -1)
(tool-bar-mode -1)
(scroll-bar-mode -1)
