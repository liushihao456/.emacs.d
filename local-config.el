;; This file is added to .gitignore. It is meant to setup per machine locally in
;; order to add extra decoupled configurations.

(defvar theme-to-load 'base16-dracula "Theme to load.")

(defvar font-to-use "Operator Mono")
(defvar font-size-to-use "21")

(provide 'local-config)

;; Local Variables:
;; flycheck-disabled-checkers: (emacs-lisp-checkdoc)
;; End:
